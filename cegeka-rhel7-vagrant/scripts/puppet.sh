# puppet is already installed via the kickstart script on the Jeos ISO
# we don't have to install it manually
#
# yum install puppet

# install the deep merge gem, used by hiera
gem install deep_merge
